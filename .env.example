# Database (Prisma) - use ./dev.db (DB at project root) to avoid nested prisma/prisma/ when cwd varies. Run all commands from project root.
DATABASE_URL="file:./dev.db"

# NextAuth
NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET="generate-a-random-secret-with-openssl-rand-base64-32"

# Vercel Blob (required for payslip uploads on Vercel). Create a Blob store in Vercel → Storage, then add BLOB_READ_WRITE_TOKEN.
# BLOB_READ_WRITE_TOKEN="vercel_blob_rw_..."

# Cron: set CRON_SECRET in Vercel for scheduled payslips (Vercel Cron sends Authorization: Bearer CRON_SECRET).
# CRON_SECRET="random-secret"