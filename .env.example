# Database (Prisma) - use ./dev.db (DB at project root) to avoid nested prisma/prisma/ when cwd varies. Run all commands from project root.
DATABASE_URL="file:./dev.db"

# NextAuth
NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET="generate-a-random-secret-with-openssl-rand-base64-32"

# SMTP settings for sending payslip emails (use any SMTP server)
# If you get "read ETIMEDOUT" or "ESOCKET": check host/port, try port 465 with SMTP_SECURE=true, or firewall/network blocking outbound SMTP.
#
# --- Option A: Your own SMTP server ---
# Set your server host, port, and credentials. Use SMTP_SECURE=true for port 465 (TLS).
# SMTP_HOST=mail.yourdomain.com
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=no-reply@yourdomain.com
# SMTP_PASS=your-smtp-password
# SMTP_FROM=no-reply@yourdomain.com
#
# --- Option B: Gmail ---
# Enable 2-Step Verification, then create an App Password at https://myaccount.google.com/apppasswords
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=your-email@gmail.com
# SMTP_PASS=your-16-char-app-password
# SMTP_FROM=your-email@gmail.com

SMTP_HOST=smtp.sendgrid.net
SMTP_PORT=465
SMTP_SECURE=true
SMTP_USER=apikey
SMTP_PASS=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
SMTP_FROM=rawvitaltd@gmail.com
